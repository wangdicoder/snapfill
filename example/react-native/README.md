# @snap-fill/demo-react-native

Expo example app for testing the `@snap-fill/react-native` adapter against a sample checkout page.

## Run

```bash
pnpm install
pnpm build
cd example/react-native
npx expo start --clear
```
